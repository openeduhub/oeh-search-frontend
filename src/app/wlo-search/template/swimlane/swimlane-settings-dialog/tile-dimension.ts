export class TileDimension {
    numberOfRows: number;
    numberOfCols: number;

    constructor(rows: number, cols: number) {
        this.numberOfRows = rows;
        this.numberOfCols = cols;
    }
}
