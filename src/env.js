// When the application is deployed using Docker, this file will be overridden on container startup
// to reflect environment variables passed to the `docker run` command.
(function (window) {
    window.__env = window.__env || {};
})(this);
