query GetEntry($id: ID!, $language: Language!) {
    get(id: $id, language: $language) {
        ...searchHit
        collections {
            name
            url
            numberElements
            # thumbnail {
            #     mimetype
            #     image
            # }
            # color
        }
    }
}
