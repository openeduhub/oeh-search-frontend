// Please copy this file to `env.js` uncomment lines below for local dev settings.
//
// When the application is deployed using Docker, `env.js` will be overridden on container startup
// to reflect environment variables passed to the `docker run` command.
(function (window) {
    window.__env = window.__env || {};
    // window.__env.RELAY_URL = 'http://localhost:3000';
})(this);
