// Settings for the Web App
//
// Please copy this file to `env.js` and uncomment lines below for local dev settings.
//
// When the application is deployed using Docker, `env.js` will be overwritten on container startup
// to reflect environment variables passed to the `docker run` command.
(function (window) {
    window.__env = window.__env || {};
    // window.__env.WORDPRESS_URL = 'https://wirlernenonline.de';
    // window.__env.SHOW_EXPERIMENTS = 'false';
    // window.__env.ANALYTICS_URL = 'http://localhost:3000';
})(this);
